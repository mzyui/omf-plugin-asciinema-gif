#!/bin/bash
# This script is executed when the plugin is installed via Oh My Fish.
# For this plugin, functions are automatically loaded by OMF from the 'functions' directory,
# so no special actions are needed here.
